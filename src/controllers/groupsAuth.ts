/*declare var require: any; // This "fixes" some error with require

const jwt = require('jsonwebtoken'),
    cryp = require('crypto'),               // was crypto, but gave naming error
    User = require('../model/user'),
    config = require('../config/config');*/

// Is above already defined once???

// Gets all the info in a specific group
export const get_group = function(req, res, next){

}

// Creates a new group
export const new_group = function(req, res, next){

}

// Creates a new message in the group chat
export const message = function(req, res, next){

}

// Creates a new event in the groups event list
export const event = function(req, res, next){

}

// Edit a message in the group chat
export const edit_message = function(req, res, next){

}

// Edit an event in the group event list
export const edit_event = function(req, res, next){

}

export const delete_group = function(req, res, next){

}

export const delete_event = function(req, res, next){

}

